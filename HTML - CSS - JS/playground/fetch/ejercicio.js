// Fetchear Productos
fetch('url');

// Crear una función que reciba productos y cree los nodos correspondientes
const createProducts = (product = {}) => {};

// Crear una función que reciba un array de productos, itere sobre los mismos y los vaya agregando al HTML
const iterateAndCreate = (arr = []) => {};